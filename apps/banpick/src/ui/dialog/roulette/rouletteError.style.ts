import styles from "../../pref/legacy.module.css";
import { createComponent } from "../../pref/createComponent";

export const RouletteErrorContainer = createComponent("section", styles.stack);
export const ErrorCloseBtn = createComponent("button", styles.button);
