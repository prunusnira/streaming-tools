import styles from "../pref/legacy.module.css";
import { createComponent } from "../pref/createComponent";

export const TabLayoutContainer = createComponent("section", styles.tabLayout);
export const TabPlacement = createComponent("section", styles.tabPlacement);
export const TabFragment = createComponent("section", styles.tabFragment);
export const TabChat = createComponent("section", styles.tabChat);
